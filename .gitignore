# Swift
**/.build
**/.swiftpm
apps/macos/dist

# Env
.env
.env.*
!.env.example

# Claude Code (share skills/ with the team; keep local settings private)
.claude/settings.local.json

# Signing keys — NEVER commit these
*.key
*.key.pub

# OS
.DS_Store
Thumbs.db
